import styled from 'styled-components';

const Wrapper = styled.div`
    .youtube-videos {
        .title {
            background: #01216A;
            color: white;
            text-align: center;
            padding: 10px;
        }
        iframe {
            width: 100%;
            height: 225px;
            border: none;
        }

        h4 {
            margin: 0;
        }

        button {
            padding: 0;
        }
    }
`;

export default Wrapper;
