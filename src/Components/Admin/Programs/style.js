import styled from "styled-components";

const Wrapper = styled.div`
    padding: 20px;

    .items {
        margin-bottom: 10px;

        h2 {
            margin: 0;
        }
    }
`;

export default Wrapper;
