import styled from 'styled-components';

const Wrapper = styled.div`
    .active {
        color: green;
        font-weight: bold;
    }
    .in-active {
        color: #9d1717;
    }

    .action-buttons {
        button {
            margin-right: 10px;
        }
    }
`;

export default Wrapper;